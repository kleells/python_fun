# A function that accepts a first name and a last
# name and formats it to "{last_name}, {first_name}"
# (returns a string).

def name (first_name, last_name):
  print (f"{last_name}, {first_name}")

name("Katie", "Eells")